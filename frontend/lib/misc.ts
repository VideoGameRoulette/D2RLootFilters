import type { SelectableItem } from "./types";

/** Misc items (gems, keys, etc.) for the Misc section. Extend when catalog.misc.json or data is available. */
export const MISC_ITEMS: SelectableItem[] = [];
